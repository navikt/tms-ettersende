import React from "react";
//import { useNavigate } from "react-router-dom";
import { Button } from "@navikt/ds-react";

const Tilbakeknapp = () => {
  //const navigate = useNavigate();

  /*<Button variant="secondary" onClick={() => navigate.goBack()}>
            Gå tilbake
    </Button>*/

  return <></>;
};

export default Tilbakeknapp;
